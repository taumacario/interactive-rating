import { styled } from "../../styles/stitches.config";

export const NumbersConatiner = styled('div', {
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center',
  gap: '2.2rem',
  marginTop: '3rem'
})